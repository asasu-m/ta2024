public class Coinmain_7_6
{
    public static void main(String[] args)
    {
        //CoinCase クラスのインスタンスを作成する。

        //種類と枚数を入力し、AddCoins メソッドで硬貨を追加することを 10 回繰り返す。

        /*各硬貨が何枚でいくら(何円か)あるかを表示する。
         * (GetCountとGetAmount(新しく作成したメソッド))
         * 注：新しく作成したメソッドを呼び出すときは引数の数に注意!!
         * (以前作成したメソッドとは同じ名前で引数の数が違うため)
         */


         //硬貨の総枚数を表示する。

         //総額を表示する。

        return;
    }
}