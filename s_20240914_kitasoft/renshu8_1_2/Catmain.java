public class Catmain
{
    public static void main(String[] args)
    {

        Cat cat = new Cat();//Cat クラスのインスタンスを作成した。

        /*
         *TODO:
         * public のメンバー変数、
         * Name、Age に名前、年齢を設定する。
         * 
         * public のメンバ変数には
         * 作成したインスタンス.publicのメンバ変数
         * このような形でアクセスできる
         * 
         * 例えば上でインスタンスを生成したCatクラスのcatで
         * Catクラスの
         * (Animalクラスを継承しているため
         * Animalのメンバ変数とメンバメソッドも
         * Catクラスは持っている)
         * publicなNameにアクセス(別のクラスから直接操作する。)したい場合
         * cat.Name="たまちゃん"
         * このようにアクセスできる
         * ただし、privateなメンバ変数はこのようなアクセスはできない。
         */

         //ShowProfile を実行する。

         //Sleep を実行する。

        return;
    }
}